import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    container: {
        padding: 10,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    },

    imgTeam: {
        width: "100%",
        marginBottom: 20,
    },

    nameTeam: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    },
}));
