import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    container: {
        width: "100%",
    },

    imgTeam: {
        width: "100%",
        paddingBottom: "10",
    },

    nameTeam: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    },
}));
