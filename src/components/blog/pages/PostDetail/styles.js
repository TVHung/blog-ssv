import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    container: {
        minHeight: 600,
    },

    content: {
        backgroundColor: "red",
        width: "100%",
        height: 600,
    },

    intro_suggest: {
        backgroundColor: "green",
        width: "100%",
        height: 600,
    },
}));
